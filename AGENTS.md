# AGENTS.md

## Cursor Cloud specific instructions

This is a greenfield repository ("CredVault — Cross-Platform Multi-App Selective Credential Extraction & Provisioning Tool") that currently contains only a `README.md`. No source code, build system, dependencies, or services exist yet.

- **No build/lint/test commands** are available — the repo has no `package.json`, `Makefile`, `pyproject.toml`, or equivalent.
- **No services to start** — there are no backend, frontend, or database components.
- The VM has Node.js v22, Python 3.12, and Git pre-installed.
- When code is added, update this section with the relevant build, lint, test, and run commands.
